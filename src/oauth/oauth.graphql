type Oauth {
  # Example field (placeholder)
  exampleField: Int
}

input CreateOauthInput {
  # Example field (placeholder)
  exampleField: Int
}

input UpdateOauthInput {
  id: Int!
}

type Query {
  oauth: [Oauth]!
  oauth(id: Int!): Oauth
}

type Mutation {
  createOauth(createOauthInput: CreateOauthInput!): Oauth!
  updateOauth(updateOauthInput: UpdateOauthInput!): Oauth!
  removeOauth(id: Int!): Oauth
}
